--------------HECHO-----------
MODIFICAR STATS: agregar gol en contra, penal y tarjeta azul?
- PERMITIR AGREGAR IMAGEN CREACION EQUIPO  URGENTE
agregar fecha de nacimiento jugador
agregar barrio al crear equipo (tambien al editar)
eliminar torneos, equipos, partidos, estadisticas (para eso deben mostrarse una vez se abra el coso editar partidos)
colores botones  (verde agregar, rojo eliminar, azul guardar/cambiar)
stats deben guardarse por partido (para no repetir y para mostrar x partido)
agregar seccion noticias        
subpanel o nuevo panel para cargar noticias (nuevos componentes,bdd,etc.)
mostrar apellido (o nombre completo) en vez de nombre en stats 
archivo de texto con esta data SHOW CREATE TABLE estadisticas_partido;
ver tema PJ == 1
agregar dif gol bdd? no. calcularlo dps haciendo gf-gc
MOFICIAR MYSQLEXACTO POSICIONES
agregar partidos a apertura para ver si DG está bien implementado:
ver todo de tabla de posiciones
agregar fixture a apertura
fixture !
que la fecha y hora se muestren bien y no como un choclo
cuando se crea un equipo, a tabla de pos
pedir chatgpt que pase MYSQLEXACTO a mysql

-----------POR HACER-----------

 revisar que carajo es el subrayado
-todo el front

jugadp automaticamente quizas ???
editar partido abajo del mismo partido  ????
 cuando se elimina un partido, se eliminan las stats relacionadas a él ????

 -arreglar fixture logica?



agregar paginas individuales para ver stats de equipos y quizas jugadores --------- POCO URG
modificar visualmente partidos jugados en equipoindividual

PROXIMO:
fechas equipo/id y mejorar resultados en ese panel
mejorar fixture en cuanto a lo de arriba

chequear endpoint recalcular



-----POR HACER UNA VEZ TERMINADO TODO-----
-eliminar eventos
-en cargar partido deben mostrarse solamente los equipos del torneo que se elije
-mejorar panel lucio (incluso agregar previsualizacion)
-revisar si mostrar nom y apellido o solo apellido en minuto a minuto y panel
-que se guarde la fecha de nacimiento al editar equipo
-limitar numeros (no negativos,etc.)


https://m.wcostream.tv





  const renderEventos = (partidoId: number) => {
    const eventosDelPartido = eventos
      .filter((e) => e.partido_id === partidoId)
      .sort((a, b) => a.minuto - b.minuto);

    if (eventosDelPartido.length === 0) return null;

    return (
      <ul className="minuto-a-minuto">
        {eventosDelPartido.map((e, i) => (
          <li key={i}>
            <strong>{e.minuto}'</strong>{" "}
            {e.tipo === "gol"
              ? "⚽"
              : e.tipo === "amarilla"
              ? "🟡"
              : e.tipo === "roja"
              ? "🔴"
              : "🔵"}{" "}
            {e.apellido} ({e.equipo}) -{" "}
            {e.tipo === "gol"
              ? `Gol${
                  e.tipo_gol === "en_contra"
                    ? " en contra"
                    : e.tipo_gol === "penal"
                    ? " de penal"
                    : ""
                }`
              : e.tipo === "amarilla"
              ? "Amarilla"
              : e.tipo === "roja"
              ? "Roja"
              : "Azul"}
          </li>
        ))}
      </ul>
    );
  };






